/**
 * @module solid_crockford/3_liskov_substitution/fp/before
 */

/**
 * @template {any} [T={}]
 * @public
 * @function
 * @param {Array.<T>} [entities=[]]
 */
export const execute = (entities = []) => {
  entities.forEach(en => {});
};
